# Creative Stack Backend

Express API with MongoDB persistence for website submissions.

## Run locally

1. Install dependencies:
   `npm install`
2. Create `.env` from `.env.example` and set `MONGODB_URI`.
3. Start the backend:
   `npm run dev`

The API runs on `http://localhost:5000` by default.
